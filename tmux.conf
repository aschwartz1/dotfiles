# reset prefix
unbind C-b
set -g prefix C-s

# bind config resource key
bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded"

# bind split navigation without prefix
bind-key -n C-h select-pane -L
bind-key -n C-j select-pane -D
bind-key -n C-k select-pane -U
bind-key -n C-l select-pane -R

# change split window keys
bind-key - split-window -v -c '#{pane_current_path}'
bind-key \ split-window -h -c '#{pane_current_path}'

# bind keys for resizing windows
bind -n S-Left resize-pane -L 2
bind -n S-Right resize-pane -R 2
bind -n S-Down resize-pane -D 1
bind -n S-Up resize-pane -U 1

bind -n C-Left resize-pane -L 10
bind -n C-Right resize-pane -R 10
bind -n C-Down resize-pane -D 5
bind -n C-Up resize-pane -U 5

# set new window functionality
bind c new-window -c '#{pane_current_path}'
set-option -g base-index 1
set-option -g renumber-windows on
bind-key b break-pane -d

# bind a command for the list of sessions
bind-key C-j choose-tree

# set defaults 
set-option -g default-terminal "screen-256color"
set-option -g status-keys "emacs"

# set status bar  color
#set-option -g status-bg '#666666'
#set-option -g status-fg '#aaaaaa'

# set status bar title length
set-option -g status-left-length 50
set-option -g status-right " #(date '+%a, %b %d - %I:%M') "
